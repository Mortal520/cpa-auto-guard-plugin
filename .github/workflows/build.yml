name: build

on:
  push:
    branches: [main, master]
    tags: ['v*']
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  build:
    name: Build ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            goos: linux
            goarch: amd64
            artifact: cpa-auto-guard.so

    steps:
      - name: Checkout plugin
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: false

      - name: Fetch CLIProxyAPI SDK source
        shell: bash
        run: |
          set -euo pipefail
          git clone --depth 1 https://github.com/router-for-me/CLIProxyAPI ./CLIProxyAPI-src
          echo "Replaced SDK path: $(pwd)/CLIProxyAPI-src"

      - name: Append replace to go.mod
        shell: bash
        run: |
          set -euo pipefail
          # go.mod 仓库版本不带 replace (避免提交本地路径)。CI 在此追加一行指向刚 clone 的 SDK。
          printf '\nreplace github.com/router-for-me/CLIProxyAPI/v7 => ./CLIProxyAPI-src\n' >> go.mod
          cat go.mod

      - name: Download dependencies
        shell: bash
        run: |
          set -euo pipefail
          go mod download 2>&1 | tee download.log

      - name: Build c-shared
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: '1'
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p bin
          # Skip tidy to avoid rewriting the patched replace; build will
          # resolve the replaced module from ./CLIProxyAPI-src and download
          # transitive deps via the runner GOPROXY.
          go build -buildmode=c-shared -trimpath -o "bin/${{ matrix.artifact }}" . 2>&1 | tee build.log
          ls -la bin || true

      - name: Collect build logs on failure
        if: failure()
        run: |
          echo "::group::go.mod"
          cat go.mod
          echo "::endgroup::"
          echo "::group::download.log"
          cat download.log 2>/dev/null || true
          echo "::endgroup::"
          echo "::group::build.log"
          cat build.log 2>/dev/null || true
          echo "::endgroup::"
        shell: bash

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: bin/${{ matrix.artifact }}
          if-no-files-found: warn
          retention-days: 30
          compression-level: '0'

      - name: Upload logs (always)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: logs-${{ matrix.goos }}-${{ matrix.goarch }}
          path: |
            go.mod
            download.log
            build.log
          if-no-files-found: ignore
          retention-days: 7
